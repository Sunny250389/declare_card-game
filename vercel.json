{
  "public": true,
  "installCommand": "npm ci",
  "buildCommand": "EXPO_PUBLIC_API_URL=/api npx --no-install expo export --platform web",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.py"
    }
  ]
}
