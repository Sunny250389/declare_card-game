{
  "public": true,
  "buildCommand": "npm ci && EXPO_PUBLIC_API_URL=https://declare-card-game-api.vercel.app npx --no-install expo export --platform web",
  "outputDirectory": "dist",
  "routes": [
    {
      "handle": "filesystem"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
